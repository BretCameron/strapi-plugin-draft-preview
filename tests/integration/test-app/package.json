{
  "name": "include-drafts-test-app",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal Strapi app used by the plugin's integration tests. Resolves dependencies from the plugin root's node_modules — does NOT have its own node_modules.",
  "dependencies": {
    "@strapi/plugin-graphql": "*",
    "@strapi/plugin-users-permissions": "*",
    "@strapi/strapi": "*",
    "better-sqlite3": "*",
    "strapi-plugin-include-drafts": "*"
  }
}
